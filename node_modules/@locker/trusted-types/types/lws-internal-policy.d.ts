import { ScriptElement } from '@locker/shared-dom';
import { ContentType, Evaluator } from './types';
export declare const lwsInternalPolicy: {
    createHTML(dirty: string, lwsKey: string, contentType: ContentType): string;
    createScript(_dirty: string, _evaluator: Evaluator): string;
    createScriptURL(dirty: string, evaluator: Evaluator, targetElement: ScriptElement): string;
};
//# sourceMappingURL=lws-internal-policy.d.ts.map